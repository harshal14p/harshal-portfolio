function initPremiumInteractions(){const cards=document.querySelectorAll('.tilt-card,.about-card,.skill-group,.edu-card,.cert-card,.project-card,.contact-card');const photo=document.querySelector('.hero-photo');const fine=matchMedia('(pointer:fine)').matches;if(fine){cards.forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect();card.style.setProperty('--mx',`${e.clientX-r.left}px`);card.style.setProperty('--my',`${e.clientY-r.top}px`);if(!card.classList.contains('tilt-card'))return;const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${-y*4}deg) rotateY(${x*5}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>{if(card.classList.contains('tilt-card'))card.style.transform=''})});if(photo){photo.addEventListener('pointermove',e=>{const r=photo.getBoundingClientRect();const x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;photo.style.setProperty('--shine-x',`${(x+.5)*100}%`);photo.style.setProperty('--shine-y',`${(y+.5)*100}%`);photo.style.transform=`translate3d(${x*10}px,${y*8}px,0) rotateX(${-y*6}deg) rotateY(${x*8}deg) scale(1.025)`;photo.classList.add('is-hovered')});photo.addEventListener('pointerleave',()=>{photo.style.transform='';photo.classList.remove('is-hovered')})}}const sections=[...document.querySelectorAll('main section[id]')],links=[...document.querySelectorAll('.nav-links a')];if(!sections.length||!links.length)return;const map=new Map(links.map(a=>[a.getAttribute('href')?.slice(1),a]));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){links.forEach(a=>a.classList.remove('active'));map.get(entry.target.id)?.classList.add('active')}}),{rootMargin:'-35% 0px -55% 0px',threshold:0});sections.forEach(s=>observer.observe(s))}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',initPremiumInteractions);else initPremiumInteractions();