/* Cinematic enhancement retired from runtime.
   The portfolio now keeps one stable layout system; decorative background is handled
   by js/genz-playground.js + css/background-only.css. This file intentionally does
   nothing so legacy script tags cannot reintroduce layout/typography/hero conflicts. */
(()=>{'use strict';})();
