import * as THREE from 'three';
let renderer,scene,camera,world,gada,icons,particles;
const pointer=new THREE.Vector2(),target=new THREE.Vector2();let scroll=0,scrollNow=0;
const gold=new THREE.MeshStandardMaterial({color:0xc99624,metalness:.96,roughness:.2,emissive:0x3a2102,emissiveIntensity:1.15});
const goldHi=new THREE.MeshStandardMaterial({color:0xf2ce63,metalness:1,roughness:.15,emissive:0x4a2b05,emissiveIntensity:1.4});
const blueGlow=new THREE.MeshBasicMaterial({color:0x2f8cff,transparent:true,opacity:.08,blending:THREE.AdditiveBlending,depthWrite:false});
function makeGada(){
  const g=new THREE.Group();
  const shaft=new THREE.Mesh(new THREE.CylinderGeometry(.075,.12,5.3,24),goldHi);shaft.position.y=-1.35;g.add(shaft);
  const grip=new THREE.Mesh(new THREE.CylinderGeometry(.13,.17,.85,20),gold);grip.position.y=-4.15;g.add(grip);
  const rings=[-4.55,-4.25,-3.95,-.1,.35,.82,1.05];rings.forEach((y,i)=>{const r=i<3?.19:.72;const tor=new THREE.Mesh(new THREE.TorusGeometry(r,.045,10,28),goldHi);tor.rotation.x=Math.PI/2;tor.position.y=y;g.add(tor)});
  const profile=[new THREE.Vector2(.42,1.02),new THREE.Vector2(.7,1.15),new THREE.Vector2(.98,1.48),new THREE.Vector2(1.08,1.82),new THREE.Vector2(.96,2.2),new THREE.Vector2(.7,2.48),new THREE.Vector2(.45,2.62),new THREE.Vector2(.25,2.55)];
  const head=new THREE.Mesh(new THREE.LatheGeometry(profile,32),goldHi);head.position.y=0;g.add(head);
  const crown=new THREE.Mesh(new THREE.ConeGeometry(.28,.72,20),goldHi);crown.position.y=3.0;g.add(crown);
  const collar=new THREE.Mesh(new THREE.TorusGeometry(.48,.07,12,32),gold);collar.rotation.x=Math.PI/2;collar.position.y=2.05;g.add(collar);
  const aura=new THREE.Mesh(new THREE.SphereGeometry(1.45,24,18),blueGlow);aura.position.y=1.75;g.add(aura);
  g.rotation.z=-.18;g.rotation.x=.08;g.scale.setScalar(.82);return g;
}
function makeSymbol(type){const g=new THREE.Group(),m=new THREE.MeshStandardMaterial({color:0x78baff,metalness:.8,roughness:.3,transparent:true,opacity:.3,emissive:0x0a3f80,emissiveIntensity:1.1});if(type==='film'){const body=new THREE.Mesh(new THREE.BoxGeometry(1.1,.7,.16),m);g.add(body);for(let i=-2;i<3;i++){const b=new THREE.Mesh(new THREE.BoxGeometry(.13,.12,.18),m);b.position.set(i*.22,.43,0);g.add(b)}}else if(type==='tech'){g.add(new THREE.Mesh(new THREE.OctahedronGeometry(.62,1),m));}else if(type==='music'){const n=new THREE.Mesh(new THREE.TorusGeometry(.25,.055,8,22),m);n.rotation.x=Math.PI/2;g.add(n);const s=new THREE.Mesh(new THREE.BoxGeometry(.07,1,.07),m);s.position.set(.22,.25,0);g.add(s)}else{g.add(new THREE.Mesh(new THREE.IcosahedronGeometry(.52,1),m))}return g}
function makeParticles(){const n=750,a=new Float32Array(n*3);for(let i=0;i<n;i++){const r=7+Math.random()*17,t=Math.random()*Math.PI*2;a[i*3]=Math.cos(t)*r;a[i*3+1]=(Math.random()-.5)*18;a[i*3+2]=(Math.random()-.5)*12}const geo=new THREE.BufferGeometry();geo.setAttribute('position',new THREE.BufferAttribute(a,3));return new THREE.Points(geo,new THREE.PointsMaterial({color:0x5aaaff,size:.022,transparent:true,opacity:.32,blending:THREE.AdditiveBlending,depthWrite:false}))}
function resize(){if(!renderer)return;camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight,false)}
function move(e){target.x=e.clientX/innerWidth-.5;target.y=e.clientY/innerHeight-.5}function scr(){scroll=Math.min(1,Math.max(0,scrollY/Math.max(1,document.documentElement.scrollHeight-innerHeight)))}
export function initScene(){const c=document.getElementById('scene-canvas');if(!c)return;scene=new THREE.Scene();scene.fog=new THREE.FogExp2(0x02060b,.032);camera=new THREE.PerspectiveCamera(40,innerWidth/innerHeight,.1,90);camera.position.set(0,0,15);renderer=new THREE.WebGLRenderer({canvas:c,alpha:true,antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight,false);renderer.outputColorSpace=THREE.SRGBColorSpace;world=new THREE.Group();scene.add(world);gada=makeGada();gada.position.set(-2.6,2.5,-3);scene.add(gada);const specs=[['film',4.8,1.8,-4,.65],['tech',4.9,-1.6,-3,.62],['music',-4.9,.3,-4.2,.65],['game',1.8,3.3,-6,.55]];icons=specs.map(([t,x,y,z,s],i)=>{const o=makeSymbol(t);o.position.set(x,y,z);o.scale.setScalar(s);o.userData={x,y,z,p:i*.8};scene.add(o);return o});particles=makeParticles();scene.add(particles);const key=new THREE.PointLight(0x3c95ff,14,28);key.position.set(3,5,8);scene.add(key);const warm=new THREE.PointLight(0xffc84a,3.5,16);warm.position.set(-4,2,4);scene.add(warm);scene.add(new THREE.AmbientLight(0x173250,.7));addEventListener('resize',resize,{passive:true});addEventListener('mousemove',move,{passive:true});addEventListener('scroll',scr,{passive:true});scr();const clock=new THREE.Clock();function tick(){const t=clock.getElapsedTime();pointer.lerp(target,.045);scrollNow+=(scroll-scrollNow)*.025;gada.rotation.y=-.18+pointer.x*.16;gada.rotation.x=.08+pointer.y*.07+scrollNow*.32;gada.rotation.z=-.18+Math.sin(t*.25)*.025;gada.position.y=2.5-scrollNow*4.8;gada.position.x=-2.6+pointer.x*.28;icons.forEach((o,i)=>{const b=o.userData;o.rotation.x=pointer.y*.15+Math.sin(t*.3+b.p)*.08;o.rotation.y=t*(.07+i*.012)+pointer.x*.2;o.position.x=b.x+pointer.x*.35;o.position.y=b.y+Math.sin(t*.25+b.p)*.15-scrollNow*(.65+i*.12);});particles.rotation.y=t*.004+pointer.x*.06;world.rotation.y=pointer.x*.07;camera.position.x+=(pointer.x*.7-camera.position.x)*.025;camera.position.y+=(-pointer.y*.45-camera.position.y)*.025;camera.position.z+=(15+scrollNow*1.5-camera.position.z)*.02;camera.lookAt(0,0,0);renderer.render(scene,camera);requestAnimationFrame(tick)}tick()}
